package five1;

public class WageCalculator {
    public double computeWage(WorkerInfo worker) {
        String jobTitle = worker.getJobTitle();
        double wage = 0.0;
        System.out.println("Computing wage for job title: " + jobTitle);
        return wage;
    }
}
